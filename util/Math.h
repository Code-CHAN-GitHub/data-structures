//
// Created by chen on 2021/9/4.
//

#ifndef DATA_STRUCTURES_MATH_H
#define DATA_STRUCTURES_MATH_H

#define max(x, y) ((x > y) ? (x) : (y))
#define min(x, y) ((x < y) ? (x) : (y))

#endif //DATA_STRUCTURES_MATH_H
